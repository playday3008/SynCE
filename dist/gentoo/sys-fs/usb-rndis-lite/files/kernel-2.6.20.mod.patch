diff -Naur usb-rndis-lite/rndis_host.c usb-rndis-lite_2.6.20/rndis_host.c
--- usb-rndis-lite/rndis_host.c 2007-02-25 16:46:22.000000000 -0500
+++ usb-rndis-lite_2.6.20/rndis_host.c  2007-02-18 23:20:13.000000000 -0500
@@ -575,7 +575,7 @@
        struct rndis_halt       *halt;

        /* try to clear any rndis state/activity (no i/o from stack!) */
-       halt = kcalloc(1, sizeof *halt, SLAB_KERNEL);
+       halt = kcalloc(1, sizeof *halt, GFP_KERNEL);
        if (halt) {
                halt->msg_type = RNDIS_MSG_HALT;
                halt->msg_len = ccpu2(sizeof *halt);
diff -Naur usb-rndis-lite/usbnet.c usb-rndis-lite_2.6.20/usbnet.c
--- usb-rndis-lite/usbnet.c     2007-02-25 16:46:22.000000000 -0500
+++ usb-rndis-lite_2.6.20/usbnet.c      2007-02-18 23:21:11.000000000 -0500
@@ -180,9 +180,9 @@
        period = max ((int) dev->status->desc.bInterval,
                (dev->udev->speed == USB_SPEED_HIGH) ? 7 : 3);

-       buf = kmalloc (maxp, SLAB_KERNEL);
+       buf = kmalloc (maxp, GFP_KERNEL);
        if (buf) {
-               dev->interrupt = usb_alloc_urb (0, SLAB_KERNEL);
+               dev->interrupt = usb_alloc_urb (0, GFP_KERNEL);
                if (!dev->interrupt) {
                        kfree (buf);
                        return -ENOMEM;
@@ -1150,7 +1150,7 @@
        skb_queue_head_init (&dev->done);
        dev->bh.func = usbnet_bh;
        dev->bh.data = (unsigned long) dev;
-       INIT_WORK (&dev->kevent, kevent, dev);
+       INIT_WORK (&dev->kevent, kevent);
        dev->delay.function = usbnet_bh;
        dev->delay.data = (unsigned long) dev;
        init_timer (&dev->delay);


$Id$

TODO for synce-serial
=====================

o Add option to autorespawn synce with an entry in /etc/inittab:

  ce:2345:respawn:/usr/bin/synce-serial-start

o Make the chat script wait a few seconds for "CLIENT" if neccessary and
  ignore other data it receives.
	
o Make synce-serial-start verify that dccm is running

